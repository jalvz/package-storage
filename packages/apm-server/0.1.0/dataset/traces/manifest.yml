title: APM traces
release: experimental
type: events # events / metrics / logs

# elasticsearch:
#   index_template.mappings:
#     dynamic: false
#   index_template.settings:
#     index.lifecycle.name: some-name
